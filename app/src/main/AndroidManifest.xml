<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.tiecode.plugin.autocode">

    <application
        android:name=".AutoCodePlugin"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:targetApi="31" >

        <meta-data
            android:name="tiecode_plugin"
            android:value="true" />
        <meta-data
            android:name="plugin_name"
            android:value="结绳中文速码" />
        <meta-data
            android:name="plugin_versionCode"
            android:value="1" />
        <meta-data
            android:name="plugin_versionName"
            android:value="1.0.1" />
        <meta-data
            android:name="plugin_targetVersion"
            android:value="1" />
        <meta-data
            android:name="plugin_author"
            android:value="结绳项目团队" />
        <meta-data
            android:name="plugin_note"
            android:value="结绳中文速码插件，减少您的代码量！本插件提供三个注解属性[数据模型类, 等值对象类, 序列化类]，可用于快速生成代码" />
    </application>

</manifest>